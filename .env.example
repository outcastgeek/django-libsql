# Example environment configuration for django-libsql
# Copy this file to .envrc and update with your values
# Then run 'direnv allow' to load the environment

# Turso database configuration
TURSO_DATABASE_URL="libsql://your-database.turso.io"
TURSO_AUTH_TOKEN="your-turso-auth-token"
TURSO_SYNC_INTERVAL="0.1"

# Optional: Encryption key for embedded replicas
# TURSO_ENCRYPTION_KEY="your-encryption-key"

# Python settings
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1

# Optional: Disable GIL for performance testing (Python 3.13+)
# PYTHON_GIL=0